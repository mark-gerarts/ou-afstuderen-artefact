module.exports = {
    // Other browsers can be tested as well by adding 'webkit' and/or 'firefox'
    // here.
    browsers: ['chromium'],
    // Should we ever put the whole project in a container, we could use
    // `serverOptions` here to start the application before running the tests.
}
